name: Deploy dbadmin-site to Pages

on:
  push:
    paths:
      - 'sites/dbadmin-site/**'

jobs:
  deploy:
    uses: ./.github/workflows/_deploy-sites.yml
    with:
      siteDirectory: sites/dbadmin-site
      projectName: admin-gr8r
    secrets:
      CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
