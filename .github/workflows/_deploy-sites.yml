name: Global Pages deploy

on:
  workflow_call:
    inputs:
      workingDirectory:
        required: true
        type: string
      buildDirectory:
        required: false
        type: string
      projectName:
        required: true
        type: string
    secrets:
      CF_API_TOKEN:
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy ${{ inputs.projectName }} to Cloudflare Pages

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Log context
        run: |
          echo "Branch: ${{ github.ref }}"
          echo "Commit: ${{ github.sha }}"
          echo "workingDirectory = ${{ inputs.workingDirectory }}"
          echo "buildDirectory = ${{ inputs.buildDirectory }}"
          echo "projectName     = ${{ inputs.projectName }}"

      - name: Install Wrangler
        run: npm install -g wrangler@4.26.0

      - name: Build site
        run: |
          BUILD_DIR="${{ inputs.buildDirectory }}"
          if [ -z "$BUILD_DIR" ]; then
            BUILD_DIR="${{ inputs.workingDirectory }}"
          fi

          echo "📦 Building site in: $BUILD_DIR"
          cd "$BUILD_DIR"

          if [ -f package.json ]; then
            npm install || true
            npm run build
          else
            echo "⚠️ No package.json found in $BUILD_DIR. Skipping build."
          fi

      - name: Deploy to Cloudflare Pages
        run: |
          export CLOUDFLARE_API_TOKEN=${{ secrets.CF_API_TOKEN }}
          export CLOUDFLARE_ACCOUNT_ID=6a22f9b8ad27ffcc9719dd53a78bcedf

          BUILD_DIR="${{ inputs.buildDirectory }}"
          if [ -z "$BUILD_DIR" ]; then
            BUILD_DIR="${{ inputs.workingDirectory }}"
          fi

          echo "📁 Final dist path: $BUILD_DIR/dist"
          ls -la "$BUILD_DIR/dist" || exit 1

          wrangler pages deploy "$BUILD_DIR/dist" --project-name=${{ inputs.projectName }} --commit-dirty=true

