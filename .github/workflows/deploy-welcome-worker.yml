name: Deploy welcome-worker

on:
  push:
    branches:
      - main
    paths:
      - 'workers/welcome-worker/**'
      - '.github/workflows/welcome-worker.yml'
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/_deploy-worker.yml
    with:
      workingDirectory: 'workers/welcome-worker'
      workerName: 'gr8r-welcome-worker'
    secrets:
      CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}